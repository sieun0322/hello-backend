syntax = "proto3";
package market_data;

message SubscribeRequest {
  string symbol = 1;
}

message TradeEvent {
  string symbol = 1;
  uint64 price = 2;
  uint64 quantity = 3;
  uint64 timestamp = 4;
}

service MarketData {
  rpc Subscribe(SubscribeRequest) returns (stream TradeEvent);
}
